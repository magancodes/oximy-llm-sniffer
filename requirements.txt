# Core detector dependencies
numpy
pandas
scikit-learn
joblib

# scapy parses pcaps in PURE PYTHON (replaces tshark) and also synthesizes the
# demo pcaps. Because there is no system binary to install, the detector runs
# the same way locally and in a serverless environment (e.g. Vercel).
scapy

# Web portal (app.py)
flask
