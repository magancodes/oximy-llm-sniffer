# Python
__pycache__/
*.pyc

# Regenerable data (recreate with: python make_fixtures.py). Not needed at
# runtime: a trained model.joblib IS committed so serverless deploys (Vercel)
# work with no training step and no fixtures on disk.
ai.csv
normal.csv
fixtures/

# PP Mondwest pixel display font (Pangram Pangram), used by the portal UI.
# The loose root copy stays ignored; the one the UI serves from static/ is tracked.
*.otf
!static/PPMondwest-Regular.otf
